<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetMealMapper">
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO setmeal (name, category_id, price, status, description, image, create_time, create_user, update_time, update_user)
        VALUES (#{name}, #{categoryId}, #{price}, #{status}, #{description}, #{image}, #{createTime}, #{createUser}, #{updateTime}, #{updateUser})
    </insert>
    <insert id="insertSetmealDishes">
        INSERT INTO setmeal_dish (setmeal_id, dish_id, copies,price,name)
        VALUES
        <foreach collection="setmealDishes" item="item" separator=",">
            (#{item.setmealId}, #{item.dishId}, #{item.copies}, #{item.price}, #{item.name})
        </foreach>
    </insert>


    <update id="update">
        UPDATE setmeal
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="image != null">
                image = #{image},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser}
            </if>
        </set>
        WHERE id = #{id}
    </update>
    <select id="selectPage" resultType="com.sky.vo.SetmealVO">
        SELECT s.*,c.name as categoryName FROM setmeal s left join category c on s.category_id = c.id
        <where>
            <if test="name != null and name != ''">
                AND s.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="categoryId != null">
                AND s.category_id = #{categoryId}
            </if>
            <if test="status != null">
                AND s.status = #{status}
            </if>
        </where>
        order by s.update_time desc
    </select>

</mapper>
